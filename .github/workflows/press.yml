name: clockwork
on:
  schedule:
    - cron: "0,15,30,45 * * * *"   # the clock, until cron-job.org points here; github may run it a few minutes late
  workflow_dispatch:
    inputs:
      job: { description: "press | dry | doctor | claimcheck | holders | tgcheck", default: press, type: choice, options: [press, dry, doctor, claimcheck, holders, tgcheck] }
permissions: { contents: write }
concurrency: { group: clockwork, cancel-in-progress: false }
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with: { node-version: 22 }
      # an exact version, never @latest: the machine key is in this step's environment, so the code that
      # runs here is only ever a build we published with provenance and you chose by hand. to update,
      # change the number (docs/PUBLISH.md in the clockwork repo lists releases).
      # two clocks can fire within a minute of each other; the machine's own guard reads its last run from the
      # checkout, and a checkout can be a few seconds behind the previous run's push. so ask github directly:
      # if another tick committed its numbers less than ten minutes ago, this one leaves before it signs anything.
      - name: the clock's guard
        id: guard
        env: { GH_TOKEN: "${{ github.token }}" }
        run: |
          git pull -q --rebase origin main || true
          last=$(gh api "repos/${{ github.repository }}/commits?path=data/press-stats.json&per_page=1" --jq '.[0].commit.committer.date' 2>/dev/null || echo "")
          if [ -n "$last" ] && [ "${{ inputs.job || 'press' }}" = "press" ]; then
            age=$(( $(date +%s) - $(date -u -d "$last" +%s) ))
            if [ "$age" -lt 600 ]; then echo "another tick committed its numbers ${age}s ago; leaving"; echo "skip=1" >> "$GITHUB_OUTPUT"; fi
          fi
      - if: steps.guard.outputs.skip != '1'
        run: npx --yes clockwork-press@0.2.2 ${{ inputs.job || 'press' }}
        env:
          npm_config_ignore_scripts: "true"   # no package lifecycle scripts run on install; only the clockwork bin runs
          MACHINE_WALLET_KEY: ${{ secrets.MACHINE_WALLET_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}   # only for telegram mode "own"; leave the secret unset in shared mode
      # the feed runs on the same clock, from its own wallet. it stays a dry run that signs nothing until the
      # repository variable FEED_LIVE is set to 1, which is a switch in settings, not a code change.
      - if: steps.guard.outputs.skip != '1'
        continue-on-error: true
        run: npx --yes clockwork-press@0.2.9 feed
        env:
          npm_config_ignore_scripts: "true"
          FEEDER_WALLET_KEY: ${{ secrets.FEEDER_WALLET_KEY }}
          CLOCKWORK_FEED_LIVE: ${{ vars.FEED_LIVE }}
      # always: what a failed run wrote before it failed (a claim row, the legs of a slice, lastRunAt) is the
      # truth, and the next tick's interval guard reads it from this commit.
      - name: commit the numbers
        if: always()
        run: |
          git config user.name "clockwork"; git config user.email "clockwork@users.noreply.github.com"
          git add data/ && (git diff --cached --quiet || (git commit -qm "clockwork: update" && git pull -q --rebase origin main && git push -q origin main))
